# Phase 0 Checklist

## P0

- [x] 桌面端图标资源迁移到新目录
- [x] 删除旧版 `selfme/` 目录、旧迁移文档和构建残留
- [x] 删除 Gateway / Web / Desktop / TUI 中所有 legacy / compat 路径，彻底切到最新协议与最新设计
- [x] 清理所有“为迁移过渡服务”的垃圾代码、兼容字段、旧命名和临时说明

## 功能回归

- [x] 以 Web 为基准端，手动验证发送、排队、取消、重连、错误态
- [x] 用同一份回归清单验证 Desktop
- [ ] 用同一份回归清单验证 TUI
- [ ] 确认 metadata、时序、取消竞态在三端一致
- [ ] 重点复测队列串行发送：必须等待上一条完整结束后才发送下一条

## Gateway

- [x] 继续美化日志输出，明确协议、模型、会话、耗时、错误层级
- [x] 收敛协议设计，只保留最终版 API / WebSocket 事件
- [x] 保留必要 smoke，删除过渡期兼容测试假设

## Desktop

- [x] 检查窗口图标、应用图标、Dock / 任务栏图标是否都正常
- [x] 确认资源路径、开发启动、生产启动都稳定
- [x] 补最小 macOS 打包链路，先产出本地可运行 `.app`
- [x] 打包态桌面只保留 `settings.json`，运行参数改为内部固定托管

## Web / Desktop UI

- [x] 完成 Web 主界面重设计：导航、消息区、输入区、空屏、设置页、元信息
- [x] 清理旧版视觉遗留：流式高亮闪烁、旧状态文案、过重按钮、冗余说明文案
- [x] 收敛 Web 元信息与状态表达：`Streaming`、`Cancelled`、`Error`、`tokens + model`
- [x] 将当前 Web 视觉完整同步到 Desktop
- [x] 用 Desktop 实机验证当前共享 UI 的表现

### Web 已完成

- [x] 左侧导航定稿：仅保留 `Chat` / `Model settings` 与版本信息
- [x] 顶部导航定稿：仅保留连接状态与模型信息
- [x] `Model settings` 页面可滚动，字段层级已收敛
- [x] API key 显示为脱敏摘要，不回显明文
- [x] 空屏重做为功能化起始面板
- [x] 消息区层级、代码块、流式状态、输入区工作台样式已重做
- [x] `New messages` 位置改为随输入区高度动态联动
- [x] 队列发送逻辑改为严格串行的前端队列

## TUI

- [ ] 重做 TUI 界面美化
- [ ] 优化消息层次、状态提示、流式动画、排队展示
- [ ] 确认 Enter / Ctrl+Enter / Esc / Ctrl+C / exit 行为都稳定

## 工程清理

- [ ] 决定注释语言规范：中文、英文，或分层规则
- [ ] 统一注释规范并补齐不清晰的代码说明
- [x] 按当前架构重写 README
- [ ] 清理无用 docs，只保留当前有效文档
